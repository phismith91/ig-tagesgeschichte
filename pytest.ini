[pytest]
# ponytail: unterdrückt ein global installiertes, projektfremdes pytest-socket-Plugin
# (aus pytest-homeassistant-custom-component), das echte Sockets blockt — curate_server-Tests
# brauchen einen echten localhost-Server.
addopts = -p no:homeassistant
